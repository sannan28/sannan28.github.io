参考资料:https://blog.csdn.net/weixin_46075832/article/details/126703474
1.kafka的定义
   Kafka传统定义：Kafka是一个分布式的基于发布/订阅模式的消息队列（MessageQueue），主要应用于大数据实时处理领域。
      发布/订阅：消息的发布者不会将消息直接发送给特定的订阅者，而是将发布的消息分为不同的类别，订阅者只接收感兴趣的消息。
   Kafka最新定义：Kafka是一个开源的分布式事件流平台（Event Streaming Platform），被数千家公司用于高性能数据管道、流分析、数据集成和关键任务应用。
   消息队列：
     目前企业中比较常见的消息队列产品主要有 Kafka、ActiveMQ 、RabbitMQ 、RocketMQ 等。在大数据场景主要采用 Kafka 作为消息队列。在 JavaEE 开发中主要采用 ActiveMQ、RabbitMQ、RocketMQ。
   传统消息队列的应用场景
     传统的消息队列的主要应用场景包括：缓存消峰、解耦和异步通信。
     缓存削峰:有助于控制和优化数据流经过系统的速度，解决生产消息和消费处理速度不一致的情况。
     解耦:允许你独立扩展或修改两边的处理过程，只要确保他们同样遵守同样的接口约束
     异步通信:
   消息队列的两种方式
     点对点：
     发布订阅
      区别: 点对点消费 -> 消息只能发布到一个主题， 消费完成就删除消息，且只有一个消费者
      发布订阅模式 -> 消息可以发布到多个主题， 消息一般保留七天，且有多个消费者


 
2.kafka 生产者
   (1).生产者的发送原理:
        在消息发送的过程中，涉及到两个线程，main线程和sender线程，其中main线程是消息的生产线程，而sender线程是jvm单例的线程，专门用于消息的发送。
        在jvm的内存中开辟了一块缓存空间叫RecordAccumulator（消息累加器），用于将多条消息合并成一个批次，然后由sender线程发送给kafka集群。
        我们的一条消息在生产过程会调用send方法然后经过拦截器经过序列化器，再经过分区器确定消息发送在具体topic下的哪个分区，然后发送到对应的消息累加器中，
        消息累加器是多个双端队列。并且每个队列和主题分区都具有一一映射关系。消息在累加器中，进行合并，达到了对应的size（batch.size）或者等待超过对应的等待时间(linger.ms)，
        都会触发sender线程的发送。sender线程有一个请求池，默认缓存五个请求（ max.in.flight.requests.per.connection ），发送消息后，会等待服务端的ack，如果没收到ack就会重试默认重试int最大值（ retries ）。
        如果ack成功就会删除累加器中的消息批次，并相应到生产端。      
   (2).关于生产者发送数据
         需求：创建 Kafka 生产者，采用异步的方式发送到 Kafka Broker
         1.普通发送
         2.异步带回调的发送
         3.同步发送API
   (3).生产者拦截器
   (4).生产者分区
         从存储的角度 -> 合理使用存储资源，实现负载均衡
         从计算的角度 -> 提高并行计算的可行性
         Kafka支持三种分区策略 1) 指定分区； 2）指定key，计算hash得分区； 3）指定随机粘性分区；
   (5).生产者提高吞吐量 -> 通过提高吞吐量达到低延迟的效果-> Batch.size 与 linger.ms 配合使用，根据生成数据的大小指定。RecordAccumlator：在异步发送并且分区很多的情况下，32M的数据量容易被满足，进程交互加大，可以适当提高到64M。
        1.消息累加器（RecordAccumulator）
            
   (6).kafka 生产者端是如何保证消息不重复的
   一句话总结：
     幂等性：通过 pid + partition + seqNumber 去重，解决单分区消息重复问题。
     事务机制：通过原子提交以及read_commitited隔离级别，解决跨分区的重复和部分成功的问题。
   为什么消息会重复？
    1.生产者重试：网络波动时，已发送但未收到确认的消息会被重发。
    2.消费者重复消费：消费成功后 offset 未及时提交，消费者重启后重新消费。
   什么样的消息属于重复的 判断的标准是什么 -> 标准就是:pid + partitopn + SeqNumber 
     说明：那么 pid 代表的就是 producerId, kafka 但是每次重启都会重新分配一个新的producerId。
          partition 代表的是分区号。
          SeqNumber 序列号 生产者会给每一个消息贴的一个递增的编号 目的就是让消息有序。
   broker是如何判断消息是否重复的呢？
     broker broker 会在内存中维护一个pid+partition+seqNumer,如果接收到的消息比seqNumer>1 就存储消息，如果小于seqNumer 就说明消息重复;如果消息大于1 以上，就说明消息丢失了。就会抛异常。
  幂等性:解决单分区内重复
   而幂等的原理是什么呢？
     开启幂等之后，
       1.生产者会为每条消息附加一个pid 以及 seqNumber 以及 partition,
       2.broker 会记录每个pid 在每个分区最新序列号。如果收到重复的序列号，直接丢弃。
        所以幂等解决的就是 sender 到 broker 间。由于网络波动可能造成的重发问题。用幂等来标识唯一消息。
    注意：幂等性只能保证的是在 单分区+单会话内 不重复。
    场景示例：
     生产者发送订单消息 订单A（PID=1, Seq=1）到 orders 主题的 Partition 0。
     Broker 收到后记录 PID=1, Partition 0, Seq=1。
     如果生产者未收到 ACK 并重发 订单A（PID=1, Seq=1），Broker 发现序列号已存在，直接丢弃重复消息。
      效果：单分区内即使生产者重试，消息也不会重复。

 事务机制: 解决跨分区的原子性和重复
   原理：
     事务机制保证开启多个分区的操作是原子的(要么全成功，要么全失败)。
     生产者开启事务后，所有的消息会暂时存储到事务日志中，提交时才会对消费者可见。
   例子：
    // 生产者代码示例
   producer.beginTransaction();
   try {
      // 发送订单消息到 orders 主题的 Partition 0
      producer.send(new ProducerRecord<>("orders", "订单A"));
      // 发送库存扣减消息到 inventory 主题的 Partition 1
      producer.send(new ProducerRecord<>("inventory", "扣减库存A"));
      producer.commitTransaction(); // 提交事务
      } catch (Exception e) {
      producer.abortTransaction(); // 回滚事务
    }
   执行的过程:
     1.事务开启后，两条消息会写入各自分区，但被标记为“未提交”（消费者不可见）。
     2.提交事务时，Kafka 会写入一条 COMMIT 标记到事务日志。
     3.消费者配置 isolation.level=read_committed 后，只会读取已提交的消息。
   效果:
    如果事务提交前生产者崩溃，所有消息会被回滚（消费者看不到）。
    如果生产者重试并成功提交，消息只会被写入一次。
其他问题的思考：
   如果producer重新启动 那么还能保证消息不重复吗？ 若Producer重启，新的实例会检测到旧事务未完成并终止，避免消息重复。
   消息重试后分区的选择？如果为启动幂等性,重试时可能因负载均衡策略变化，导致消息被发送到其他 Partition。启用幂等时， Producer 会严格确保消息发送到原 Partition，避免因分区切换导致重复。

那么如何配置幂等以及事务
   启用幂等
     1.显式地将生产者客户端参数enable.idempotence设置为true,即可(这个参数的默认值为true)。
     2.且还需要确保生产者客户端的retries、acks、max.in.filght.request.per.connection参数不被配置错，默认值就是对的
   启用事务


 (7).消息的顺序性
    消息在单分区内有序，多分区内无序，如果要对多分区排序，造成分区无法工作需要等待排序，浪费性能。
      如果未开启幂等性，需要 max.in.flight.requests.per.connection 设置为1。
      如果开启幂等性，需要 max.in.flight.requests.per.connection 设置为小于5。
      这是因为broker端会缓存producer主题分区下的五个request，保证最近5个request是有序的。
    
3.broker
   1.副本的故障处理
       如果follower落后leader过多，体现在落后时间repca.lag.time.max.ms,follower就会被移除ISR队列,等待该队列LEO追上HW,才会重新加入ISR中。
       leo(log end offset):每个副本的最后一个offset,leo其实就是最新的offset+1。
       hw(high watermark):所有副本中最小的leo。
     故障处理的流程：
       (1)follower 发生故障之后会被临时踢出ISR。
       (2)这个期间leader和follower继续接收数据。
       (3)待该follower恢复后，follower会读取本地磁盘记录的上次hw,并将log文件高于hw的部分截取掉，从hw向leader进行同步。
       (4)等该follower的leo大于等于该partition的hw,也就是follower追上leader之后，就可以重新加入ISR了。

   2.leader的故障流程
       (1).旧Leader先被从ISR队列中踢出，然后从ISR中选出一个新的Leader来；
       (2).此时为了保证多个副本之间的数据一致性，其他的follower会先将各自的log文件中高于HW的部分截取掉，然后从新的leader同步数据（由此可知这只能保证副本之间数据一致性，并不能保证数据不丢失或者不重复）。体现了设置ACK-all的重要性。

   3.分区副本分配
   4.存储结构
       在Kafka中主题（Topic）是一个逻辑上的概念，分区（partition）是物理上的存在的。每个partition对应一个log文件，该log文件中存储的就是Producer生产的数据。Producer生产的数据会被不断追加到该log文件末端。为防止log文件过大导致数据定位效率低下，Kafka采用了分片和索引机制，将每个partition分为多个segment，每个segment默认1G（ log.segment.bytes ）， 每个segment包括.index文件、.log文件和**.timeindex**等文件。这些文件位于文件夹下，该文件命名规则为：topic名称+分区号。
   5.文件清理策略
       Kafka将消息存储在磁盘中，为了控制磁盘占用空间的不断增加就需要对消息做一定的清理操作。Kafka 中每一个分区副本都对应一个Log，而Log又可以分为多个日志分段，这样也便于日志的清理操作。Kafka提供了两种日志清理策略。
        日志删除(delete) :按照一定的保留策略直接删除不符合条件的日志分段。
        日志压缩(compact) :针对每个消息的key进行整合，对于有相同key的不同value值，只保留最后一个版本。

    6.kafka高效读数据
       1.kafka是分布式集群，采用分区方式，并行操作
       2.读取数据采用稀疏索引，可以快速定位消费数据
       3.顺序写磁盘
           Kafka 的 producer 生产数据，要写入到 log 文件中，写的过程是一直追加到文件末端，为顺序写。官网有数据表明，同样的磁盘，顺序写能到 600M/s，而随机写只有 100K/s。这与磁盘的机械机构有关，顺序写之所以快，是因为其省去了大量磁头寻址的时间。
       4.页缓冲和零拷贝
           kafka高效读写的原因很大一部分取决于页缓存和零拷贝
            (1)页缓存
                在Kafka 中，大量使用了 PageCache， 这也是 Kafka 能实现高吞吐的重要因素之一。
​                首先看一下读操作，当一个进程要去读取磁盘上的文件内容时，操作系统会先查看要读取的数据页是否缓冲在PageCache 中，如果存在则直接返回要读取的数据，这就减少了对于磁盘 I/O的 操作；
                但是如果没有查到，操作系统会向磁盘发起读取请求并将读取的数据页存入 PageCache 中，之后再将数据返回给进程，就和使用redis缓冲是一个道理。
​                接着写操作和读操作是一样的，如果一个进程需要将数据写入磁盘，操作系统会检查数据页是否在PageCache 中已经存在，如果不存在就在 PageCache中添加相应的数据页，接着将数据写入对应的数据页。
                另外被修改过后的数据页也就变成了脏页，操作系统会在适当时间将脏页中的数据写入磁盘，以保持数据的一致性。具体的刷盘机制可以通过 log.flush.interval messages，log.flush .interval .ms 等参数来控制。同步刷盘可以提高消息的可靠性，防止由于机器 掉电等异常造成处于页缓存而没有及时写入磁盘的消息丢失。一般并不建议这么做，刷盘任务就应交由操作系统去调配，消息的可靠性应该由多副本机制来保障，而不是由同步刷盘这 种严重影响性能的行为来保障 。
            (2)零拷贝
                 零拷贝并不是不需要拷贝，而是减少不必要的拷贝次数，通常使用在IO读写过程中。常规应用程序IO过程如下图，会经过四次拷贝：
                   1.数据从磁盘经过DMA(直接存储器访问)到内核的Read Buffer；
                   2.内核态的Read Buffer到用户态应用层的Buffer
                   3.用户态的Buffer到内核态的Socket Buffer
                   4.Socket Buffer到网卡的NIC Buffer
                 从上面的流程可以知道内核态和用户态之间的拷贝相当于执行两次无用的操作，之间切换也会花费很多资源；当数据从磁盘经过DMA 拷贝到内核缓存（页缓存）后，为了减少CPU拷贝的性能损耗，操作系统会将该内核缓存与用户层进行共享，减少一次CPU copy过程，同时用户层的读写也会直接访问该共享存储，本身由用户层到Socket缓存的数据拷贝过程也变成了从内核到内核的CPU拷贝过程，更加的快速，这就是零拷贝。
                 甚至如果我们的消息存在页缓存PageCache中，还避免了硬盘到内核的拷贝过程，更加一步提升了消息的吞吐量。 (大概就理解成传输的数据只保存在内核空间，不需要再拷贝到用户态的应用层)。Java的JDK NIO中方法transferTo()方法就能够实现零拷贝操作，这个实现依赖于操作系统底层的sendFile()实现的。

5.Consumer(kafka的消费者)
    1.漏消费和重复消费
       重复消费：已经消费了数据，但是 offset 没提交。
         自动提交引起的。
         场景： 
          1.Consumer每5s提交offset。
          2.如果提交offset后的2s,consumer挂了。
          3.再次重启consumer,则上一次提交的offset处继续消费，导致重复。
       漏消费：先提交 offset 后消费，有可能会造成数据的漏消费。
         设置offset为手动提交，当offset 被提交的时，数据还在内存中未落盘,此时刚好消费者线程被kill掉，那么offset已经提交，但是数据未处理，导致这部分内存中的数据丢失。
         场景：
          1.提交offset。
          2.消费者消费的数据还在内存中，消费者挂掉，导致漏消费。
     2.消费者事务
         如果想完成Consumer端的精准一次性消费，那么需要kafka消费端将 消费过程和提交offset过程做原子绑定,此时我们需要kafka的offset保存到支持事务自定义的介质(mysql)
     3.消息积压(提高吞吐量)
        (1).如果kafka消费能力不足，则可以考虑增加topic的分区数，同时提升消费组的消费者数量,消费者数=分区数。二者缺一不可。
        (2).如果是下游的数据处理不及时：提高每批次拉取数据量。批次拉取数据过少(拉取数据/处理时间<生产速度)，使处理的数据小于生产的数据，也会造成生产积压。
      fetch.max.bytes 默认Default: 52428800（50 m）。消费者获取服务器端一批消息最大的字节数。
      如果服务器端一批次的数据大于该值（50m）仍然可以拉取回来这批数据，因此，这不是一个绝对最大值。一批次的大小受 message.max.bytes （broker config）or max.message.bytes （topic config）影响。
      max.poll.records 一次 poll 拉取数据返回消息的最大条数，默认是 500 条

     4.关于消费者或者消费者组 如何确定 要处理哪个分区的消息  专业点来说 Kafka分区分配策略(Partition Assignment Strategy) 
        Range
          工作原理:
             对每个Topic单独计算分配
             将Topic的分区按数字顺序排序，消费者按字典序排序
             计算每个消费者应分配的分区数：分区数/消费者数
             余数部分分配给前几个消费者
         复杂场景示例：
           假设有：
             主题：T1(5个分区), T2(3个分区), T3(4个分区)
             消费者：C1, C2, C3
         所有消费者都订阅所有主题
            分配计算过程：
              主题T1 (5个分区)
                分区: [0,1,2,3,4], 消费者: [C1,C2,C3]
                每个消费者基础分配数: 5/3 = 1...余数2
                分配结果:
                 C1: [T1-0, T1-1]  (基础1个 + 余数1个)
                 C2: [T1-2, T1-3]  (基础1个 + 余数1个)  
                 C3: [T1-4]        (基础1个)
            最终分配：
              C1: [T1-0,T1-1, T2-0, T3-0,T3-1]  → 5个分区
              C2: [T1-2,T1-3, T2-1, T3-2]      → 4个分区  
              C3: [T1-4, T2-2, T3-3]           → 3个分区
            问题：C1比C3多分配了67%的分区，负载不均衡！

        RoundRobin
         工作原理：将所有Topic的分区及消费者按字典序排序后，逐个轮询分配给消费者
         计算方式：
           将所有Topic的所有分区混合在一起排序
           将所有消费者按字典序排序
           按顺序轮询分配给消费者
   复杂场景示例：
     同样使用上面的场景，但假设消费者订阅不同的主题：
      C1订阅: [T1, T2]
      C2订阅: [T1, T3]
      C3订阅: [T2, T3]
   分配计算过程：
     所有分区排序：[T1-0, T1-1, T1-2, T1-3, T1-4, T2-0, T2-1, T2-2, T3-0, T3-1, T3-2, T3-3]
     消费者排序：[C1, C2, C3]
     轮询分配（跳过未订阅的情况）：
   最终分配：
     C1: [T1-0, T1-2, T1-4, T2-1]        → 4个分区
     C2: [T1-1, T1-3, T3-0, T3-2]        → 4个分区
     C3: [T2-0, T2-2, T3-1, T3-3]        → 4个分区
   问题解决：分配均衡！但前提是RoundRobin要求消费者订阅相同的Topic。

        Sticky
         计算方式：
           初始分配：力求均衡，类似RoundRobin
           再平衡时：尽量保留现有的分配，只移动必要的分区
         复杂场景示例：
           初始状态（同RoundRobin结果）：
             C1: [T1-0, T1-2, T1-4, T2-1]
             C2: [T1-1, T1-3, T3-0, T3-2]  
             C3: [T2-0, T2-2, T3-1, T3-3]
          场景：消费者C3宕机，触发再平衡
            传统策略（非Sticky）可能的结果：
              C1: [T1-0, T1-2, T1-4, T2-0, T2-1, T3-1]  → 6个分区
              C2: [T1-1, T1-3, T2-2, T3-0, T3-2, T3-3]  → 6个分区
            问题：大量分区移动，影响消费连续性
            Sticky策略的结果：
             C1: [T1-0, T1-2, T1-4, T2-1, T2-0, T3-1]  → 保留了4个原有分区
             C2: [T1-1, T1-3, T3-0, T3-2, T2-2, T3-3]  → 保留了4个原有分区
            优势：C1和C2各自保留了4个原有分区，只新增了C3的2个分区
         Cooperative Sticky 
               计算方式：
                 分配算法与Sticky相同
                 关键区别：再平衡过程是增量式的
               复杂场景示例：
                 同样C3宕机的场景，但展示再平衡过程的不同：
                   传统Sticky的再平衡：
                     停止所有消费者
                     撤销所有分区分配
                     重新计算新分配方案
                     应用新方案
                        阶段1 - 准备再平衡：
                        C1: [T1-0, T1-2, T1-4, T2-1]        ← 继续消费这些分区
                        C2: [T1-1, T1-3, T3-0, T3-2]        ← 继续消费这些分区
                        C3: [已离线]
                        
                        阶段2 - 增量分配：
                        协调器计算：需要将C3的[T2-0, T2-2, T3-1, T3-3]分配给C1和C2
                        采用最小移动原则分配：
                        C1新增: [T2-0, T3-1]
                        C2新增: [T2-2, T3-3]
                        
                        最终结果：
                        C1: [T1-0, T1-2, T1-4, T2-1, T2-0, T3-1]
                        C2: [T1-1, T1-3, T3-0, T3-2, T2-2, T3-3]
                        核心优势：在再平衡期间，未被重新分配的分区可以继续消费，实现了"无缝"的再平衡。
               推荐：在生产环境中，优先使用 Cooperative Sticky 策略，它在均衡性和再平衡效率方面都表现最佳。

实战应用需要考虑的一些问题：
   如何配置broker的数量？这个属于核心的架构问题
     配置Broker的数量不是一个随意的决定，而是基于一系列理论依据和业务需求的权衡。
       一. 理论依据：配置多个Broker的核心依据基于Kafka的三个基本特性：高可用性（High Availability）、可扩展性（Scalability） 和持久性与性能（Durability & Performance）
         1. 高可用性 (High Availability) - 容错（Fault Tolerance）
              核心问题：如果整个Kafka集群只有一个Broker，当这个Broker宕机（如机器故障、断电、维护）时，整个消息系统将完全不可用，导致业务中断。
              解决方案：通过配置多个Broker（副本）来实现冗余。Kafka的副本机制（Replication）允许将每个分区的数据复制到多个Broker上。
              理论依据：你需要配置至少3个Broker 来满足最基本的HA需求。
                假设你设置副本因子（replication.factor）为3，这意味着每个分区的数据都有3个副本(一个leader+ 2follower)，分布在3个不同的Broker上。
                这样，即使挂掉1个Broker（N-1），集群仍然可以继续提供服务（剩余2个副本达成共识）。如果挂掉2个，集群将不可用（无法达成多数派）。
                因此，为了容忍 f 个节点故障，你需要至少 2f + 1 个Broker（参考Paxos/Raft分布式共识算法）。要容忍1个节点故障，至少需要3个节点；要容忍2个，至少需要5个，以此类推。

        2. 可扩展性 (Scalability) - 提升吞吐量（Throughput）
             核心问题：单个Broker的I/O和处理能力有上限（CPU、内存、磁盘、网络）。当消息吞吐量（TPS）超过单个机器的能力时，会成为系统瓶颈。
             解决方案：通过增加Broker来进行水平扩展（Scale-out）。Topic可以被划分为多个分区（Partition），而每个分区可以被放置到不同的Broker上。生产者可以将消息并行地发送到不同分区的Leader上，消费者也可以并行地从不同分区读取消息。
             理论依据：Broker的数量决定了集群整体的I/O处理能力上限。更多的Broker意味着可以创建更多的分区来分散读写负载，从而支撑更高的并发和更大的数据量。

        3. 持久性与性能 (Durability & Performance)
             更多的副本意味着数据的安全性更高，数据丢失的风险更低。
             同时，Follower副本可以分担Leader副本的读请求（取决于客户端配置），从而提升整个集群的读性能。

      二. 实践指南：配置broker的数量没有固定的公式，但是可以遵循以下决策流程和考量因素
        第1步：明确高可用性（HA）要求
          这是最低要求。对于任何生产环境：
            最低配置：3个Broker。这允许你设置 replication.factor=3，并容忍1个节点故障。这是最常见的生产环境起步配置。
            关键业务/金融场景：5个Broker或更多。为了容忍2个或更多节点同时故障。
        第2步：评估负载与性能要求（容量规划） 这决定了你需要多大的集群来处理负载。
          1.估算峰值吞吐量：
             写入吞吐量：预计每秒要写入多少MB（或条）消息？
             读取吞吐量：预计每秒要读取多少MB（或条）消息？
          2.估算所需分区总数：
             Kafka的性能单位是分区。单个分区在每个Broker上是有性能上限的（特别是顺序I/O）。
             一个通用的经验法则：单个Broker上最好不要超过 2000~4000 个分区，否则管理和性能都会下降。更保守的建议是1000-2000个。
            所需分区总数 ≈ 目标吞吐量 / 单个分区的吞吐能力。例如，你希望达到100MB/s的写入速度，而测试发现一个分区大约能处理10MB/s，那么你大约需要10个分区来分担负载。
          3.评估单个Broker的能力：
              你的机器配置如何？（高性能SSD？万兆网卡？）这决定了单个Broker能有效处理的分区数量和吞吐量。
              另一个经验法则：一个配置良好的Broker（如：8核CPU、32GB内存、高性能SSD）通常可以处理 几十MB/s甚至上百MB/s 的吞吐量和数千个分区。
        第3步：综合计算与微调
          初步数量：所需Broker数量 ≈ ceil(总分区数 / 单个Broker理想分区数(1000-4000))
          取最大值：将上述计算结果与第1步的HA要求（如3）进行比较，取两者中的最大值。
            额外考量：
              预算：更多的机器意味着更高的成本。
              运维复杂度：集群越大，运维难度越高。
              未来扩展：预留一些资源。例如，计算出来需要3.5个Broker，建议直接配置4个或5个，为业务增长留出缓冲空间。
      最终建议：对于大多数中小型生产系统，从一个3个Broker的集群开始，设置副本因子为3，然后根据监控到的实际负载（如CPU、磁盘I/O、网络流量）再决定是否需要扩容以及如何扩容。

        配置示例（基于Spring Boot的Kafka配置）请参考我的代码链接。

如何正确计算分区数？属于容量规划以及性能测试范畴的问题 
   计算分区数是一个容量规划和性能测试的过程，没有唯一的标准答案，但可以遵循以下方法和考量因素。
     1. 核心考量因素
         a. 目标吞吐量 (Throughput)
              这是最重要的因素。你需要估算生产者和消费者的吞吐量
                生产者角度：所需分区数 >= 目标写入吞吐量 / 单个分区生产者吞吐量
                消费者角度：所需分区数 >= 目标消费吞吐量 / 单个分区消费者吞吐量
                经验值：一个配置良好的分区（如使用SSD磁盘）通常可以处理：
                  写入：每秒几MB到几十MB（例如 10-50 MB/s）
                  消费：每秒几千到几万条消息
                  示例：如果你的目标写入速率是 100 MB/s，你预估单个分区能处理 20 MB/s，那么你至少需要 100 / 20 = 5 个分区。
         b. 消费者并行度 (Consumer Parallelism)
              一个分区只能被一个消费者组内的一个消费者实例消费。这是Kafka的核心规则。
                消费者的最大数量 <= 分区数
                如果你想通过增加消费者实例来提升消费速度，你必须拥有足够多的分区。例如，你希望一个消费者组有10个实例并行消费，那么主题至少需要有10个分区。
               
         c. 未来扩展性 (Future Growth)
              一次性创建大量分区可能对集群有影响，但增加分区是一个相对昂贵的操作（虽然Kafka支持，但可能会破坏消息的Key与分区的映射关系）。因此，在初始规划时应该预留一些缓冲。
              建议：在计算出的最低分区数基础上，增加 20%-50% 的余量。
         d. 可用性和数据分布
              更多的分区意味着数据可以更均匀地分布在集群的所有Broker上，更好地实现负载均衡。
     2. 计算公式（经验模型）
          一个常用的估算公式是：
          N = max(T_p, T_c) * (1 + Buffer)
            N: 最终建议的分区数。
            T_p: 基于生产吞吐量计算的值 (目标生产吞吐量 / 单分区生产吞吐能力)。
            T_c: 基于消费吞吐量和并行度计算的值 (max(目标消费吞吐量 / 单分区消费吞吐能力, 期望的消费者数量))。
            Buffer: 缓冲系数 (例如 0.2 代表 20% 的余量)。
          实战示例：一个直播间送礼主题
             1.业务场景：峰值期间，预计每秒产生 50,000 条礼物消息，平均每条消息 1KB。
             2.吞吐量计算：
                 消息条数：50,000 msg/s
                 数据大小：50,000 * 1KB ≈ 50 MB/s
             3.性能假设（基于测试或经验）
                 单个分区处理能力：5,000 msg/s 或 5 MB/s
             4.消费者规划：希望最多有 20 个消费者实例来并行处理这些消息（例如20个微服务实例）
             5.分区计算：
                 基于生产吞吐量：T_p = 50,000 / 5,000 = 10 个分区
                 基于消费吞吐量：T_p = 50 MB/s / 5 MB/s = 10 个分区
                 基于消费者并行度：T_c = 20 个消费者 → 需要 至少20个分区
                 max(T_p, T_c) = max(10, 20) = 20
                 增加20%缓冲：N = 20 * 1.2 = 24
             6.最终决策：建议为该主题创建 24 到 30 个分区。
     3. 分区数过多的负面影响
          不是分区越多越好！过多的分区会导致:
            更高的端到端延迟：Kafka控制器（Controller）选举分区Leader、管理元数据开销更大。
            更多的文件句柄：每个分区在磁盘上对应多个文件段，会消耗大量文件句柄。 
            更长的故障恢复时间：Broker宕机后，需要为更多的分区进行Leader重新选举和数据同步
            更多的客户端内存消耗：生产者、消费者需要维护更多分区的元数据。
      一般建议：对于一个集群，单个Broker上的分区总数最好不超过 2000-4000 个。
    4.总结与最佳实践
        场景                分区数建议             说明
        开发/测试环境           1-3             简单够用即可。
        低吞吐生产环境          3-6             例如：日志收集、低频业务事件。
        中高吞吐生产环境	    12 - 50+          最常见的范围。例如：用户行为追踪、订单消息、直播间聊天/礼物。
        极高吞吐场景            100+            例如：大型互联网公司的核心数据管道。需要非常精细的性能调优。

       决策流程：
         定目标：明确你的吞吐量（msg/s或MB/s）和消费者并行度目标。
         做测试：对单个分区进行压测，了解其在你硬件上的性能极限。
         做计算：使用上述公式进行初步估算。
         留缓冲：在计算结果上增加一定的余量（例如20%-50%）。
         做验证：上线后密切监控集群性能（CPU、IO、延迟），根据实际情况进行调整。

如何构建一个极高可靠性kafka的消息传递系统？ 虽然不能从数学上证明是100% 但是在工程实践上，只要基础设置(网络，磁盘)稳定，以下的配置是能够达到的最高级别了可靠性了。
   可靠性性消息需要具备的条件： 不丢失  不重复  即使重复 也只执行一次
   producer： 
     需要配置:
       1.ack = -1(all) 等待所有ISR副本确认 想要达到的效果就是消息不丢失
       2.retries: 自动处理瞬时错误 相当于是失败重试  也是同样为了保证消息不丢失 但是同时也带来了消息重复的问题 
       3.enable.idempotence=true 这个是处理生产者端消息重复的设置 生产者端幂等 默认就是开启幂等的。如何确定消息的重复 判断的依据 pid + partition + seqnumber   
      在producer端还会有些延伸的问题  比如幂等只能保证才broker不重启的情况下处理消息的重复问题 如果broker重启 意味着pid重新生成了 所以 这个时候幂等就无法解决消息重复的问题了。
      如果broker重启破坏了幂等 那就启动生产者事务 那么事务是如何解决broker重启消息重复的问题呢？ 主要采用的是两阶段提交+持久化事务日志 其实依赖的是磁盘的数据，只要磁盘没有损坏 还数能恢复。
      如果磁盘出现问题 那还可以使用副本 这就是为什么要配置多个副本的原因。
   broker:
     需要配置：
       1.unclean.leader.election.enable=false	禁止数据丢失的副本进行选举
       2.replication.factor>=3   副本的数量   主要是为了保证 单个Broker宕机导致数据不可用或丢失
       3.min.insync.replicas=2   定义写入成功的最小副本数(就是可以leader保持同步的副本数量)  为了避免 所有副本宕机时，生产者仍可写入，潜在数据丢失风险
   consumer:
     需要配置:
       1.enable.auto.commit=false 关闭自动提交主要为了预防 处理失败但位移已提交，消息丢失
       2.手动同步提交位移；说明：确保位移提交成功，异步提交失败导致重复消费
       3.先处理业务，再提交位移  核心原则：宁可重复，不可丢失	顺序颠倒会导致消息丢失
       4.消费逻辑实现幂等性 说明 主要就是应对消息重复的问题 
     详细的我后面会写详细的代码来展示  思路先放这。

    
     
  
  
  





























     
     
