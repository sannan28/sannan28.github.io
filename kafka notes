1.

2.kafka 生产者端是如何保证消息不重复的 
   为什么消息会重复？
    1.生产者重试：网络波动时，已发送但未收到确认的消息会被重发。
    2.消费者重复消费：消费成功后 offset 未及时提交，消费者重启后重新消费。

写在前面 这里有几个概念需要了解下
   什么样的消息数据属于重复? pid + partitopn + SeqNumber
     pid 代表的就是 producerId kafka 每次重启都会重新分配一个新的producerId
     partition 代表的是分区号
     SeqNumber 序列号 生产者会给每一个消息贴的一个递增的编号 目的就是让消息有序。
broker是如何判断消息是否重复的呢？ broker 会在内存中维护一个pid+partition+seqNumer,如果接收到的消息比seqNumer>1 就存储消息，如果小于seqNumer 就说明消息重复;如果消息大于1 以上，就说明消息丢失了。就会抛异常。
  所以幂等解决的就是 sender 到 broker 间。由于网络波动可能造成的重发问题。用幂等来标识唯一消息。
    注意：并且幂等性只能保证的是在 单分区+单会话内 不重复。
